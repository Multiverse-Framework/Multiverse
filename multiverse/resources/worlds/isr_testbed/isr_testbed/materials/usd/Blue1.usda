#usda 1.0
(
    defaultPrim = "Blue1"
    upAxis = "Z"
)

def Material "Blue1"
{
    token inputs:frame:stPrimvarName = "st"
    token outputs:surface.connect = </Blue1/PBRShader.outputs:surface>

    def Shader "PBRShader"
    {
        uniform token info:id = "UsdPreviewSurface"
        color3f inputs:diffuseColor.connect = </Blue1/DiffuseTexture.outputs:rgb>
        color3f inputs:emissiveColor = (0, 0, 0)
        float inputs:opacity = 1
        int inputs:useSpecularWorkflow = 1
        token outputs:surface
    }

    def Shader "PrimvarReader"
    {
        uniform token info:id = "UsdPrimvarReader_float2"
        token inputs:varname.connect = </Blue1.inputs:frame:stPrimvarName>
        float2 outputs:result
    }

    def Shader "DiffuseTexture"
    {
        uniform token info:id = "UsdUVTexture"
        asset inputs:file = @../../textures/Wood_Floor_Dark.png@
        float2 inputs:st.connect = </Blue1/PrimvarReader.outputs:result>
        float3 outputs:rgb
    }
}

